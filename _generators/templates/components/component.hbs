import React from 'react'
import './{{pascalCase name}}.scss'

export interface propsModel {
  title?: string
}

const {{pascalCase name}} = (props: propsModel) => {
  return (
    <div className="{{pascalCase name}}">
      <h2>{props.title}</h2>
    </div>

  )
}

export default {{pascalCase name}}
